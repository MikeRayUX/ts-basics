{
  //  Conditionals
  if (mapOfIds[30] === "D") {
  }

  // For
  for (let i = 0; i < 10; i++) {
    console.log(i);
  }

  // forEach()
  [1, 2, 3].forEach((item) => console.log(item));
  // map()
  const out = [4, 5, 6].map((item) => item * 10);
}
